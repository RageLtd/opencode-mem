# Rust Tooling

Always run both `rustfmt` and `clippy` against Rust source code when finishing work.

## Commands

```
cargo fmt
cargo clippy -- -D warnings
```

## Installation

If not already installed, suggest adding them via rustup:

```
rustup component add rustfmt clippy
```
